//スタイルをグローバルに適用
import { createGlobalStyle } from "styled-components";

export default createGlobalStyle
    `
    html,body,#root{
        margin:0;
        padding:0,
        height:100%,
        width:100%,
        pasition : relative;
    }
`;